// This file re-exports types from shared/schema.ts
// so they can be easily imported in frontend components
export type {
  RoundState,
  UserPick,
  Player,
  GameState,
  ServerMessage,
  ClientMessage
} from "@shared/schema";
